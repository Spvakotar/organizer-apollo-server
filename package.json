{
  "name": "graphql-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "init": "prisma db push && prisma db seed",
    "dev": "nodemon",
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node build/index.js",
    "generate-client": "npx prisma generate --data-proxy",
    "migrate": "DATABASE_URL=\"$DATABASE_MIGRATE_URL\" npx prisma migrate deploy"
  },
  "prisma": {
    "schema": "src/prisma/schema.prisma",
    "seed": "ts-node src/prisma/seed"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^4.1.0",
    "apollo-server-express": "^3.10.0",
    "express": "^4.18.1",
    "graphql": "^16.5.0"
  },
  "devDependencies": {
    "@types/faker": "^6.6.9",
    "@types/node": "^18.0.6",
    "faker": "^6.6.6",
    "nodemon": "^2.0.19",
    "prisma": "^4.1.0",
    "rimraf": "^3.0.2",
    "ts-node": "^10.9.1",
    "typescript": "^4.7.4"
  }
}
