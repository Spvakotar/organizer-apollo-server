{
  "name": "graphql-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "init": "set DATABASE_URL=\"$DATABASE_MIGRATE_URL\" prisma db push && prisma db seed",
    "dev": "nodemon",
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node build/index.js",
    "generate-client": "npx prisma generate --data-proxy",
    "migrate": "set DATABASE_URL=\"$DATABASE_MIGRATE_URL\" && npx prisma migrate deploy"
  },
  "prisma": {
    "seed": "ts-node prisma/seed"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^4.1.0",
    "apollo-server-express": "^3.10.0",
    "express": "^4.18.1",
    "graphql": "^16.5.0"
  },
  "devDependencies": {
    "@faker-js/faker": "^7.3.0",
    "@types/node": "^18.0.6",
    "nodemon": "^2.0.19",
    "prisma": "^4.1.0",
    "rimraf": "^3.0.2",
    "ts-node": "^10.9.1",
    "typescript": "^4.7.4"
  }
}
